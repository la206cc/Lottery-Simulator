# 2026-08-02 工作日志

## 项目探索
- 用户确认这是一个彩票模拟项目，主要开发目录是 v3.0
- 项目结构完整，包含前端界面、后端服务器和核心模拟逻辑
- 支持8种官方彩种，具有完全配置化的架构
- 性能指标：每秒12万+轮模拟

## 开发工作
- 创建了 API 服务器 (api.py)，提供 RESTful 接口
  - GET /api/presets - 获取预设列表
  - GET /api/config/<id> - 获取配置
  - POST /api/simulate - 执行模拟
  - POST /api/analyze - 分析结果
  - POST /api/generate-numbers - 生成号码
  - GET /api/health - 健康检查

- 创建了模拟界面 (ui/html/simulator.html)
  - 彩票选择下拉框
  - 模拟参数配置（轮次、资金、奖池）
  - 策略选择（随机、固定、追热号）
  - 结果显示（摘要卡片、详细表格）

- 创建了前端逻辑 (ui/js/simulator.js)
  - 加载预设配置
  - 调用 API 执行模拟
  - 显示模拟结果
  - 导出功能

- 更新了主页链接，"开始模拟"按钮指向新页面

- 创建了启动脚本 (start.bat)

## 测试结果
- API 服务器正常启动
- 预设列表接口正常
- 模拟接口正常工作
- 所有功能测试通过

## Bug 修复
- 修复了模拟后页面无法滚动的问题
- 原因：config-editor.css 中 .main-content 设置了 overflow: hidden
- 解决方案：在 simulator.html 中为 .main-content 添加 overflow-y: auto

## 功能优化
- 修复了模拟逻辑问题：单次开奖模式
- 新增两种模拟模式：
  - single_draw: 单次开奖，所有投注基于同一个开奖号码（默认）
  - multi_draw: 多次开奖，每轮生成新的开奖号码
- 更新了前端界面，添加模式选择
- 优化了单次开奖模式显示：
  - 开奖号码在顶部单独大号显示
  - 表格只显示投注号码，不重复显示开奖号码
  - 每轮投注结果都是基于同一个开奖号码计算

## Bug 修复 (16:58)
- 用户反馈单次开奖模式不起作用，表格中每行开奖号码仍不同
- 根本原因：修改代码后未重启服务器，旧进程(PID 27188, 18028)仍运行旧代码
- 解决方案：杀掉旧进程，重启 API(5000)和静态文件(8080)服务器
- 额外优化：添加 JS/CSS 缓存清除参数 `?v=3` 防止浏览器缓存旧 JS

## 重规划
- 完成 v3.0 重新规划，融合 v2.0 深色主题+侧边栏架构 与 docs 参数调节面板
- 设计 4 个 Tab：开奖模拟、投注模拟(含分人群滑块面板)、数据分析、规则配置
- 8 种彩种差异化参数支持（来自 docs 完整数据）
- 规划文档：v3.0/PLAN.md

## 完整实现规格 (11:27)
- 完成 SPEC.md：覆盖所有开发细节的实现规格文档
- 包含：文件结构、CSS设计系统、HTML架构(每个Tab完整DOM)、JS模块(含核心算法伪代码)、API契约(含请求/响应格式)、8彩种参数预设完整数据、所有组件规格、后端改造清单
- 关键修复：simulators.py 奖金计算调用真实 calculator、calculators.py 移除 eval 安全漏洞、api.py 合并静态文件服务
- 10阶段实现顺序

## v3.0 完整开发完成 (12:10)
- 按 SPEC.md 完成全部10个阶段的开发和测试
- **新建9个文件**: css/style.css (595行), js/params.js, js/app.js, js/draw.js, js/bet-sim.js, js/analysis.js, index.html (340行重写)
- **重构3个文件**: api.py (合并静态服务,端口5000), simulators.py (修复奖金计算调用真实calculator), calculators.py (安全移除eval)
- **删除**: server.py (已被api.py合并)
- **测试通过**: 全部静态文件200, API健康/预设/配置/模拟正常, 单次开奖逻辑验证所有轮次号码相同
- **4个Tab功能完整**: 开奖模拟(选号+开奖+历史+CSV)、投注模拟(参数面板+滑块联动+本地兜底模拟+汇总)、数据分析(Chart.js频率/遗漏/趋势/奇偶图)、规则配置(跳转编辑器)
- **8彩种**: 参数预设全部加载, 快乐8/3D/大乐透特有选项正确显示/隐藏

## Bug修复 (12:27)
- 用户反馈三个问题，全部修复：
  1. **奖金计算错误**: 旧版用Set去重(导致重复数字彩种匹配错误)+百分比阈值(几乎全中奖)，已改为精确的按彩种逐区域匹配算法(SSQ/DLT/3D/KL8/QXC/QLC/PLW各自独立计算)
  2. **开奖号码来源**: 投注模拟现在使用开奖模拟的数据，添加期号下拉选择器，必须先开奖才能投注模拟
  3. **开奖区多余内容**: 移除投注方式/倍投/追加等投注相关按钮，选号区仅用于手选开奖号码

## Bug修复 (12:40)
- 修复三个问题:
  1. **期号选择后显示开奖号码**: 添加 `onIssueChange` 事件 + `bet-winning-display` 显示区，选择期号后立即展示该期开奖号码
  2. **清空全部按钮无效**: 添加防御性 null 检查 + try-catch，清空后同步更新投注模拟的期号选择器
  3. **开始模拟无效**: 重构 `startSimulation` 错误处理链路，API失败→本地兜底→最终catch，防止静默失败
- 所有模块 init 添加 try-catch 防止单一模块错误级联影响其他模块

## Bug修复 (13:42)
- **开始模拟卡住**: 原因是默认50万注×10000=50万轮，API fetch 无超时导致浏览器阻塞
- 解决方案: 添加 AbortController 5秒超时，超时自动切本地模拟；API请求限制最大10000轮